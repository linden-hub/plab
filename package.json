{
  "name": "plab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Playlab — a playful music & sound-design workstation for the Arturia MiniLab 3",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
